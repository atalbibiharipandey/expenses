import 'package:flutter/material.dart';

class ScreenChartPage extends StatelessWidget {
  const ScreenChartPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Chart Page")));
  }
}
